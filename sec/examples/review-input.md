# 보안 코드 리뷰 입력 예시

> 휴가 관리 시스템의 Impl 산출물 + threat-model + audit 결과를 기반으로 보안 코드 리뷰를 수행하는 입력 예시입니다.

## 구현 맵

> audit-input.md의 `implementation_map` 섹션 참조

## 코드 구조

> audit-input.md의 `code_structure` 섹션 참조

## 컴포넌트 구조

> threat-model-input.md의 `component_structure` 섹션 참조

## 위협 모델

> threat-model-output.md의 `threat_model` 섹션 참조 (TM-001 ~ TM-007)

## 취약점 보고서

> audit-output.md의 `vulnerability_report` 섹션 참조 (VA-001 ~ VA-008)
